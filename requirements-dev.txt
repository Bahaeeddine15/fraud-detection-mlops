# Dépendances de développement local uniquement (exploration, notebook,
# tracking d'expériences). PAS installées par Jenkins ni par le
# Dockerfile de production — seul requirements.txt l'est.
# Installer avec : pip install -r requirements-dev.txt -r requirements.txt

matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1
mlflow==2.17.2
imbalanced-learn==0.12.4
streamlit 
requests